#!/usr/bin/make -f

export CARGO_PROFILE=release-with-debug

%:
	dh $@

override_dh_auto_build:
	$(MAKE) PREFIX=/usr

override_dh_auto_install:
	$(MAKE) install DESTDIR=$(CURDIR)/debian/aged PREFIX=/usr

override_dh_auto_clean:
	$(MAKE) clean

override_dh_auto_test:
